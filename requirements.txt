# Runtime dependencies for the errp_bci package + notebooks.
# Kaggle's GPU image preinstalls everything except pyriemann (the orchestrator
# installs it in its first cell). Versions below are conservative floors known to
# work; the figures/tables can be reproduced from the shipped Results/ CSVs with
# only numpy + pandas + matplotlib (no GPU, torch, or datasets required).

numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.1
torch>=2.0
mne>=1.3
matplotlib>=3.6
seaborn>=0.12
tqdm>=4.64
pyriemann>=0.5
